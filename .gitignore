# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
.DS_Store
# Regenerado por xcodegen (project.yml) en cada build — no se versiona,
# así se evitan conflictos de merge en el .pbxproj (ver plan.md).
Neximed.xcodeproj/

# Metadatos de Finder generados en volúmenes exFAT/USB (p. ej. este repo en KINGSTON)
._*

# Swift Package Manager
.build/
.swiftpm/

# Temporales
*.tmp.gitconfig-local
# Config local de git de este repo (nunca se sube: credenciales/identidad locales)
.gitconfig-local
